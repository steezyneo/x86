ASSUME CS:CODE, DS:DATA
DATA SEGMENT
STR1 DB 'MICRO'
STR2 DB ?
DATA ENDS
CODE SEGMENT
START:
        MOV AX,DATA
        MOV DS,AX
        MOV ES,AX
        LEA SI,STR1
        LEA DI,STR2
        MOV CX,05
        REP MOVSB
        INT 03H
CODE ENDS
END START
