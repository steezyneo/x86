ASSUME CS:CODE, DS:DATA

DATA SEGMENT
NUM1 DB 03H
RESULT DW ?
DATA ENDS

CODE SEGMENT
START:
        MOV AX,DATA
        MOV DS,AX
        MOV AL,NUM1
        MUL AL
        MUL AL
        MOV RESULT,AX
        INT 03H
CODE ENDS
END START

